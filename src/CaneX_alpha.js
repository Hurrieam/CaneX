//CaneX_alpha

function dprint(str) {
    document.write(str);
}

function dprintln(str) {
    document.writeln(str);
}

function cprint(str) {
    console.log(str);
}

function cprintln(str) {
    console.log(str + "\n");
}
